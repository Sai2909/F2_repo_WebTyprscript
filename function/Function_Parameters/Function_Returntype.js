//function return type.
function Message(str) {
    return str.toUpperCase();
}
function Power(a, b) {
    return Math.pow(a, b);
}
var msg = Message("hello sai kiran");
var pow = Power(4, 6);
console.log(pow);
console.log(msg);
